# homebrew-cli

Homebrew tap for the Loctree CLI.

This tap should track the latest `loct` releases and provide the user-facing
install command:

```bash
brew install loctree/cli/loct
```

## Role

- formula only, no source code
- points at `Loctree/loct` release assets
- stays thin so release updates stay fast

